/// Spacing tokens from the **Rota design system**.
///
/// Source: `Rota_Design_System.pdf` — seção *Espaçamento*.
abstract final class AppSpacing {
  /// `4px` — Espaço entre ícone e texto, gap interno de chip.
  static const double space1 = 4;

  /// `8px` — Espaço entre elementos relacionados (linhas de uma lista).
  static const double space2 = 8;

  /// `16px` — Padding padrão de cards, campos e bottom sheet.
  static const double space3 = 16;

  /// `32px` — Margem de tela, espaço entre seções.
  static const double space4 = 32;
}